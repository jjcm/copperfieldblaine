= form_for @lifestyle_cue_inference_clarification_connector do |f|
  - if @lifestyle_cue_inference_clarification_connector.errors.any?
    #error_explanation
      %h2= "#{pluralize(@lifestyle_cue_inference_clarification_connector.errors.count, "error")} prohibited this lifestyle_cue_inference_clarification_connector from being saved:"
      %ul
        - @lifestyle_cue_inference_clarification_connector.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
